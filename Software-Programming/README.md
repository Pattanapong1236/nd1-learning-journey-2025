# 💻 Software Development & Tools

เครื่องมือ ภาษาโปรแกรม และสภาพแวดล้อมในการพัฒนา (IDE)

## 🛠️ Development Tools

### PlatformIO (VS Code Extension)
เครื่องมือแนะนำอันดับ 1 แทน Arduino IDE
* **ข้อดี:** ระบบจัดการ Library อัตโนมัติ (`platformio.ini`), รองรับหลายบอร์ดในโปรเจกต์เดียว, Auto-complete อัจฉริยะ

### Docker
การจำลอง Server Environment
* ใช้รัน **MQTT Broker** (Mosquitto)
* ใช้รัน **Node-RED** หรือ **InfluxDB** โดยไม่ต้องลงโปรแกรมในเครื่อง

---

## 📚 Essential Libraries (C++/Arduino)

* **PubSubClient:** สำหรับเชื่อมต่อ MQTT
* **ArduinoJson:** สำหรับจัดการข้อมูล JSON (Serialize/Deserialize)
* **Adafruit_Sensor:** ไดรเวอร์มาตรฐานสำหรับเซนเซอร์ต่างๆ
* **TaskScheduler:** สำหรับทำ Multi-tasking แบบง่ายๆ บน Arduino

---

## 🐍 Python for IoT (MicroPython / Server)
* **Paho-MQTT:** Library หลักสำหรับเขียน Python เชื่อม MQTT
* **Flask / FastAPI:** สำหรับสร้าง API Server รับค่าจาก IoT
* **Pandas:** สำหรับวิเคราะห์ข้อมูล Log ที่ส่งมาจาก Sensor
